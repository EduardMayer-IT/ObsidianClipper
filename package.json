{
  "name": "obsidian-clipper-chatgpt-fix",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.4",
    "jsdom": "^24.1.1",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  }
}
